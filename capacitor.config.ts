import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.spring',
  appName: 'ionic-spring',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
